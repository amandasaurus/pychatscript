============
Installation
============

At the command line::

    $ easy_install pychatscript

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pychatscript
    $ pip install pychatscript